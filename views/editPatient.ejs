<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Modifier Patient</title>
</head>
<body>
    <h1>Modifier Patient</h1>

    <form action="/patients/<%= patient._id %>?_method=PUT" method="POST">
        <label for="nom">Nom:</label>
        <input type="text" id="nom" name="nom" value="<%= patient.nom %>" required><br>

        <label for="prénom">Prénom:</label>
        <input type="text" id="prénom" name="prénom" value="<%= patient.prénom %>" required><br>

        <label for="dateDeNaissance">Date de Naissance:</label>
        <input type="date" id="dateDeNaissance" name="dateDeNaissance" value="<%= patient.dateDeNaissance.toISOString().split('T')[0] %>" required><br>

        <label for="sexe">Sexe:</label>
        <select id="sexe" name="sexe" required>
            <option value="Masculin" <%= patient.sexe === 'Masculin' ? 'selected' : '' %>>Masculin</option>
            <option value="Féminin" <%= patient.sexe === 'Féminin' ? 'selected' : '' %>>Féminin</option>
        </select><br>

        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= patient.email %>"><br>

        <button type="submit">Modifier Patient</button>
    </form>
</body>
</html>
